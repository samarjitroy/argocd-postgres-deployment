apiVersion: v1
kind: Secret
metadata:
  name: {{ template "argocd-postgres-helm.name" . }}-secret
type: Opaque
data:
  postgres-root-username: "{{ .Values.secret.username }}"
  postgres-root-password: "{{ .Values.secret.password }}"
